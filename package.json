{
  "name": "workspace-root",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix pdf-tools-hub",
    "build": "npm run build --prefix pdf-tools-hub",
    "start": "npm run start --prefix pdf-tools-hub"
  }
}
